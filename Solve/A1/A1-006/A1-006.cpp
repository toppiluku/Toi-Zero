#include <bits/stdc++.h>
using namespace std;
int a,b;
int main(){
    cin.tie(nullptr)->ios::sync_with_stdio(false);
    cin >> a >> b;
    if(a%b==0) cout << "yes";
    else cout << "no";
}
