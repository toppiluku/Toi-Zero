#include <bits/stdc++.h>
using namespace std;
int a,b,c;
int main(){
    cin.tie(nullptr)->ios::sync_with_stdio(false);
    cin >> a >> b >> c;
    if (a>=5 && b>=20 && c>=25) cout << "pass";
    else cout << "fail";
}
